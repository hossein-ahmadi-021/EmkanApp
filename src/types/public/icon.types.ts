export type IconNames =
  | "LogoIcon"
  | "HambergerIcon"
  | "SearchIcon"
  | "ReagonIcon";

export interface IconType {
  width: string;
  height: string;
  color?: string;
}

export type IconColorsType = "white" | "primary";
