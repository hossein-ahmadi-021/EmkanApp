export interface homeSectionDictTypes {
  infoCards: {
    btnTitle: string;
    title: string;
    subTitle: string;
    desc: string;
    id: number;
  }[];
  tabs: { title: string; id: number }[];
  scrollTitle: string;
}
